# Keys (never commit)
*.key
*.key.bin
*_key.bin
*_pub.hex

# Build artifacts
obj/
lib/
*.o
*.ali
b~*        # GNAT binder temp files (b~main.adb/ads/ali/o)
*.stdout
*.stderr
.env
/sessions/
/blog/
/docs/
.claude


*.log
temp.log

# Alire
alire/
.alire/

# Models (large binary files)
models/
*.gguf
*.model

# CUDA build artifacts (the .cu sources are tracked; built binaries/libs are not)
*.so
gpu/train_mlp
gpu/train_resident
gpu/train_shim
gpu/test_matvec
gpu/art_test
gpu/test_rmsnorm_gpu
gpu/test_swiglu_gpu
gpu/test_rope_gpu
gpu/test_attention_gpu
gpu/test_attention_mh_gpu
gpu/test_celoss_gpu
gpu/student_resident
gpu/test_gemm_tiled
gpu/test_dataparallel
gpu/dp_node
gpu/test_gemm_wmma
gpu/test_celoss_soft
gpu/test_distill_gpu

# Snaga local state
.snaga/

# IDE
*.gpr.user
*.cgpr

# Dev-only project files (not part of the build)
CLAUDE.md
debug.gpr
tests/mrope_only.gpr

# OS
.DS_Store
Thumbs.db

# P4 SVG run artifacts (dataset/targets/gguf/logs)
svgdata/
