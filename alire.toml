# Alire package manifest for Aspida
# Build: alr build

name = "aspida"
description = "E2E-encrypted LLM inference in Ada/SPARK: own GGUF engine + crypto"
version = "0.1.0-dev"
authors = ["Aspida contributors"]
licenses = "MIT"
maintainers = ["maintainers@example.com"]
website = "https://github.com/chabanov/aspida"

[[depends-on]]
gnat = "^15"
gprbuild = "*"

[configuration]
disabled = false
output_dir = "lib"
